{
    "name": "drupal/decoupled_settings",
    "description": "Exposes allowlisted simple config over JSON:API, with global values and per-consumer overrides.",
    "type": "drupal-module",
    "license": "GPL-2.0-or-later",
    "authors": [
        {
            "name": "Stuart Clark",
            "homepage": "https://www.drupal.org/u/deciphered",
            "role": "Maintainer"
        }
    ],
    "require": {
        "php": ">=8.3",
        "drupal/consumers": "^1.0",
        "drupal/jsonapi_resources": "^1.0"
    },
    "require-dev": {
        "drupal/simple_oauth": "^6"
    },
    "suggest": {
        "drupal/simple_oauth": "Identifies a frontend by its OAuth2 token, no header or query parameter needed."
    }
}
